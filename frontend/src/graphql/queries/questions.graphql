query questions {
  questions {
    id
    title
    content
  }
}

query questionsPerPage($page: Int!) {
  questionsPerPage(page: $page) {
    questions {
      id,
      title,
    },
    currentPage,
    lastPage,
    pageSize
  }
}
